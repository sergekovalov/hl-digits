{
  "name": "hl-digits",
  "version": "0.0.7",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "start": "rm -rf dist && npx tsc && node dist/index.js"
  },
  "dependencies": {
    "typescript": "^4.5.5"
  }
}
